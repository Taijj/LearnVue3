<script setup>
import { RouterLink, RouterView } from 'vue-router';
</script>

<template>
  <!--
    RouterLinks is the SPA way of loading in new conent (with Ajax)
    normal a tags will reload the entire page, when pressed
  -->
  <div>
    <nav>
      <RouterLink to="/">Home</RouterLink>
      <RouterLink to="/about">About</RouterLink>
      <RouterLink to="/contact">Contact</RouterLink>
      <RouterLink to="/lesson20">Lesson20</RouterLink>
      <RouterLink to="/lesson21">Lesson21</RouterLink>
      <RouterLink to="/lesson22">Lesson22</RouterLink>
    </nav>
  </div>

  <!-- This is where the routed content goes to -->
  <RouterView />
</template>

<style scoped>
nav {
  width: 100%;
  font-size: 12px;
  text-align: center;
  margin-top: 2rem;
}

nav a.router-link-exact-active {
  color: var(--color-text);
}

nav a.router-link-exact-active:hover {
  background-color: transparent;
}

nav a {
  display: inline-block;
  padding: 0 1rem;
  border-left: 1px solid var(--color-border);
}

nav a:first-of-type {
  border: 0;
}
</style>
